/**
 * Electron main process placeholder.
 *
 * Not wired for local run yet — install `electron` and replace this stub when
 * the Nuxt preview shell is implemented.
 */

/** @type {string} */
const PLACEHOLDER = 'PacToolkits Electron main process (placeholder)';

if (require.main === module) {
  // eslint-disable-next-line no-console
  console.error(`${PLACEHOLDER}. See apps/desktop-electron/README.md.`);
  process.exit(1);
}

module.exports = { PLACEHOLDER };
