# 位置：与本文件同目录（modules/injector/.env.local）
# test_that 脚本通过 A_ScriptDir\..\.env.local 加载；生产环境用 Host --config，不读本文件。
#
# 复制后改名：
#   cp .env.example .env.local

PG_HOST=127.0.0.1
PG_PORT=5432
PG_DB=pactoolkits
PG_USER=postgres
PG_PASS=
PG_DRIVER=PostgreSQL Unicode(x64)
PG_SSL=disable
