# 位置：与本文件同目录（modules/injector/.env.local）
# test_that 脚本通过 A_ScriptDir\..\.env.local 加载；生产由 Host 写入进程环境，不读本文件。
#
# 复制后改名：
#   cp .env.example .env.local

PAC_API_BASE_URL=http://127.0.0.1:5080
PAC_API_KEY=
# PAC_API_HEADER=X-Api-Key
# TEST_CLIENT_ID=
# TEST_DRUG_ID=盐酸氨基葡萄糖胶囊
# TEST_SPEC=0.75g*60粒
